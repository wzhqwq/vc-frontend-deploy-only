import { SimpleErrorBoundary } from "@/component/basic/errorBoundaries"

import DLEditor from "./workspace/DLEditor"

export const dlEditor = {
  Component: DLEditor,
  ErrorBoundary: SimpleErrorBoundary,
}